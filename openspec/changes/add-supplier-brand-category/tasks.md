## 1. 原型

- [x] 1.1 用 OpenPencil 在供应商原型（`docs/03-产品原型/01-主数据域/00-主数据中心/02-供应商管理/00-供应商基础信息/供应商基础信息.op`）中补：表单页「主营产品」卡片（含待确认品牌、全部品类两种状态）、详情页主营产品展示、列表页主营品牌列与两个筛选项；导出更新后的 PNG；验证：对照 spec 各场景逐一能在原型上找到
- [x] 1.2 在商品原型中补：品类卡片中的细分品类展示与带「上级品类」「中文名称」的细分品类编辑弹窗、品牌编辑弹窗的别名输入、「待确认品牌」页签与两个确认弹窗；导出 PNG；验证：同上

## 2. 数据库迁移

- [x] 2.1 新迁移（取当时下一个可用版本号）：`product_category` 加 `category_name_zh`；新建 `product_brand_alias`、`supplier_product_scope`（字段、索引、COMMENT 按 design.md 第 1 节）；验证：测试库执行成功，`SHOW CREATE TABLE` 与设计一致
- [x] 2.2 同一脚本按 design.md 第 5 节幂等写入一级、二级品类初始数据；验证：执行两次结果不变；开发库已有的 `plc` 保持不变且其商品不受影响
- [x] 2.3 同一脚本按《公司主营产品》表幂等写入品牌（官方写法 + 别名）与系列；验证：空库执行得到 4 个一级、9 个细分、24 个品牌、35 个别名、182 个系列；已有品牌以别名存在时不重复创建、系列挂到已有品牌

## 3. 后端 - 商品主数据

- [x] 3.1 品类两级规则：二级必须挂一级、禁止三级、二级中文名必填、有二级的一级不能删、被供应商主营引用的二级不能删、商品只能挂一级；`GET /categories/tree`；`options` 加 `level` 参数且默认只返回一级；验证：单测覆盖 spec「品类支持两级结构」「被引用的品类不可删除」各场景，商品现有测试全部通过
- [x] 3.2 品牌别名：维护接口、名称与别名交叉唯一校验、`BrandResolver`；被供应商主营引用的品牌不能删除；验证：单测覆盖添加别名、别名冲突、按别名解析（大小写与空格不敏感）、删除被引用品牌被拒
- [x] 3.3 待确认品牌：平台账号专用的汇总列表（跨租户，只含名称与计数）、「设为别名」「新建为品牌」及合并逻辑；验证：单测覆盖 spec「确认待确认品牌」三个场景；契约测试确认非平台账号调用被拒绝（无按钮权限返回 403；有权限但不是平台账号返回「仅平台账号可维护商品主数据」，与商品主数据其他写操作一致）

## 4. 后端 - 供应商主营产品

- [x] 4.1 `supplier_product_scope` 实体与 Mapper；供应商新增 / 更新请求加 `productScopes: [{brandId?, brandName?, categoryIds: []}]`（null 表示不修改，空列表表示清空），保存时经 `BrandResolver` 归一、校验只能选二级品类、同品牌去重、上限 50，整体替换；验证：单测覆盖 spec「维护供应商主营产品」「手填待确认品牌」各场景
- [x] 4.2 供应商详情返回主营产品（品牌名、是否待确认、品类中文名）；列表返回主营品牌摘要；分页与导出支持按品牌、品类筛选（品类筛选命中"全部品类"）；导出增加主营产品列；验证：单测与契约测试覆盖 spec「按主营品牌和品类筛选供应商」场景
- [x] 4.3 询盘快速创建（`POST /suppliers` 不带编码的路径与 `from-channel`）接受主营品牌名列表，写为全部品类；不再写 `main_brands`；验证：单测覆盖 spec「询盘快速创建供应商时选择主营品牌」
- [x] 4.4 存量 `main_brands` 迁移启动任务（幂等）；验证：单测覆盖 spec「存量主营品牌迁移」场景（含中文逗号、顿号、斜杠拆分），重复执行不产生重复行
- [x] 4.5 `mvn test` 全部通过

## 5. 前端

- [x] 5.1 商品品类页在一级卡片中展示并维护细分品类（上级品类、中文名称），品牌页别名输入与「待确认品牌」页签；验证：浏览器新增二级品类、给 Siemens 加别名「西门子」、把一个待确认品牌设为别名后该页签不再显示它
- [x] 5.2 供应商表单「主营产品」卡片、详情展示、列表列与筛选；验证：浏览器新增「Siemens：PLC、HMI」「ABB：全部品类」「Hengstler（待确认）」三行并保存，详情一致；按品牌 Siemens + 品类 PLC 筛选结果正确
- [x] 5.3 `SupplierQuickCreateModal` 主营品牌改为可输入的多选；验证：浏览器在询盘单「添加供应商」中快速创建，输入「西门子」保存后关联到 Siemens
- [x] 5.4 `npm run tsc`、Biome、`npx antd lint` 对改动文件无新问题

## 6. 上线前核对

- [x] 6.1 与平台确认生产环境现有一级品类，按 design.md 第 5 节核对初始数据里的一级编码映射，必要时调整迁移脚本；在变更里记录结论（2026-09-26 结论：种子数据以《公司主营产品》表为准，一级改为 plc / hmi / inverter / servo 四类，迁移脚本已按表格重写）

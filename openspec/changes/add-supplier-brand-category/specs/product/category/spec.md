## ADDED Requirements

### Requirement: 品类支持两级结构
系统 SHALL 支持两级品类：一级品类为独立站品类（现有品类都是一级）；二级品类必须挂在一个一级品类下，不允许再挂下级。现有商品仍只能选择一级品类；二级品类用于供应商主营产品和询盘匹配。编码唯一规则对两级品类统一适用。

#### Scenario: 创建二级品类
- **WHEN** 平台账号在一级品类 Servo 下创建编码 `servo_drive`、中文名「伺服驱动器」的品类
- **THEN** 系统创建二级品类，列表中显示在 Servo 下

#### Scenario: 不允许三级
- **WHEN** 平台账号试图在二级品类「伺服驱动器」下再创建品类
- **THEN** 系统拒绝并提示品类最多两级

#### Scenario: 商品不能选二级品类
- **WHEN** 请求把商品的品类设为二级品类「伺服驱动器」
- **THEN** 系统拒绝并提示商品只能选择一级品类

### Requirement: 品类携带中文名称
系统 SHALL 为品类提供中文名称（≤32），二级品类必填，一级品类选填；英文名称（现有品类名称）两级都必填。业务界面优先显示中文名称，独立站继续使用英文名称。

#### Scenario: 二级品类缺中文名
- **WHEN** 创建二级品类时中文名称为空
- **THEN** 系统拒绝并提示中文名称必填

### Requirement: 初始品类
系统 SHALL 在迁移时按《公司主营产品》表（`llm-wiki/raw/assets/sales/sop/01-公司主营产品.xlsx`）写入 4 个一级品类（可编程控制器、人机界面、变频器、伺服）及其下 9 个二级品类（PLC、I/O模块、通信模块、工业网络；HMI；变频器、软启动器；伺服驱动器、伺服电机）。已存在同编码的品类不重复创建，只在中文名为空时补上中文名。

#### Scenario: 迁移后可选
- **WHEN** 迁移完成后用户在供应商主营产品中选择品类
- **THEN** 可以看到按 4 个一级品类分组的 9 个二级品类

### Requirement: 被引用的品类不可删除
系统 SHALL 拒绝删除仍有二级品类的一级品类，以及被供应商主营产品引用的二级品类，提示先处理下级品类或改为停用。

#### Scenario: 删除被供应商引用的品类
- **WHEN** 平台账号删除被某供应商主营产品引用的二级品类「PLC」
- **THEN** 系统拒绝删除并提示「该品类已被供应商主营产品使用，可改为停用」

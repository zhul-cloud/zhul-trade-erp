# 方案 B · 信任蓝 + 中性灰

依据 ui-ux-pro-max 对 B2B SaaS 的推荐（信任蓝 + 中性灰、高密度数据看板）。深色为默认，**全部平面，没有任何渐变**。

## 与方案 A（现有灯塔渐变，见 `../00-商品主数据/`）的区别
- 主色：紫 → 蓝。主按钮、选中态、开关、链接都用蓝；页面底色、卡片、描边改为 slate 灰阶（不再带紫色调）
- 渐变：全部去掉（Logo、主按钮、卡片顶线、完整度进度条、关键数字、向导背景），改为纯色
- 等宽小标签（`PRODUCT MASTER` 等）保留，颜色改为中性灰
- 结构、文案、交互与方案 A 完全相同，只换视觉

## 令牌
| 用途 | 值 |
|------|-----|
| 页面 / 侧栏 / 卡片 | `#0B1220` / `#0A101D` / `#111A2C` |
| 主按钮、选中态填充 | `#2563EB`（白字 5.17:1） |
| 链接、强调文字 | `#60A5FA`（卡片上 6.84:1） |
| 控件边界 | `#64748B`（3.65:1） |
| 正文 / 次文字 / 弱文字 | `#F8FAFC` / `#CBD5E1` / `#94A3B8`（16.6 / 11.7 / 6.8:1） |

## 包含的画板（6 个关键画板，不是整套）
商品列表、商品档案（总览）、档案·FAQ 与审核、新建向导第 1 步、新建向导完成页、上传图片弹窗。

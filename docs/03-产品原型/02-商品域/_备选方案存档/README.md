# 备选方案存档

商品域视觉的四个备选方案，**2026-09-19 最终确定方案 D**。这里只留作决策记录，实现以 `../00-商品主数据/` 为准。

| 方案 | 主色 | 渐变 | 结论 |
|------|------|------|------|
| A 灯塔渐变 | 紫 → 洋红 → 青 | 满 | 冲击力最强，偏 AI 产品；渐变面积大，长时间看数据更累，不选 |
| B 信任蓝 + 中性灰 | `#2563EB` 蓝 | 无 | 最稳重，但质感偏平，不选 |
| C 单色紫，渐变限用 | `#7C3AED` 紫 | 只留主按钮和关键数字 | 折中，不选 |
| **D 信任蓝 + 渐变** | **蓝 → 靛 → 青** | **满（限用于 Logo、主按钮、卡片顶线、关键数字、进度条、向导背景）** | **选定：稳重加质感，蓝色渐变比紫色更收敛，适合财务类产品** |

- `01-方案B-…`、`02-方案C-…`：各含一个 `.op` 和 6 张关键画板 PNG
- 方案 A 的完整稿已被方案 D 替换，只保留在 `方案对比-…（A｜B｜C）.png` 里
- 方案 D 的 6 个关键画板已并入主目录的完整 29 个画板，不再单独保留
